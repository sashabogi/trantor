{
  "name": "agent-bus",
  "version": "0.10.0",
  "description": "Live message bus + auto-discovery for independent Claude Code sessions (local + cross-machine)",
  "mcpServers": {
    "relay": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp.mjs"]
    }
  }
}
