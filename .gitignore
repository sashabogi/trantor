node_modules/
*.log
# local runtime data (lives in ~/.agent-bus, never commit if a copy lands here)
.agent-bus/
bus.json
handoffs/
config.json
# secrets / env
.env
.env.*
*.pem
*.key
id_rsa*
.scratch/
# per-seat artefacts that must leave the repo but never ~/.agent-bus (#6154)
.agent-bus-out/
